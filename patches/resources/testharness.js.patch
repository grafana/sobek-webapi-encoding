diff --git a/wpt/resources/testharness.js b/wpt/resources/testharness.js
index 1a6a4bb..25835ef 100644
--- a/wpt/resources/testharness.js
+++ b/wpt/resources/testharness.js
@@ -623,32 +623,16 @@
      */
     function test(func, name, properties)
     {
-        if (tests.promise_setup_called) {
-            tests.status.status = tests.status.ERROR;
-            tests.status.message = '`test` invoked after `promise_setup`';
-            tests.complete();
-        }
-        var test_name = get_test_name(func, name);
-        var test_obj = new Test(test_name, properties);
-        var value = test_obj.step(func, test_obj, test_obj);
-
-        if (value !== undefined) {
-            var msg = 'Test named "' + test_name +
-                '" passed a function to `test` that returned a value.';
-
-            try {
-                if (value && typeof value.then === 'function') {
-                    msg += ' Consider using `promise_test` instead when ' +
-                        'using Promises or async/await.';
-                }
-            } catch (err) {}
-
-            tests.status.status = tests.status.ERROR;
-            tests.status.message = msg;
-        }
-
-        if (test_obj.phase === test_obj.phases.STARTED) {
-            test_obj.done();
+        // k6/sobek executes these baked WPT files from Go and treats script
+        // errors as test failures. The browser harness records subtest results
+        // internally, which would hide failed assertions from this runner.
+        try {
+            func();
+        } catch (e) {
+            if (name) {
+                throw `Test "${name}" failed: ${e}`;
+            }
+            throw e;
         }
     }
 
@@ -4971,5 +4955,7 @@
 }\
 ";
 
-})(self);
+// Sobek does not provide the browser/worker `self` global. Use `globalThis`
+// so the shell environment path in testharness.js can initialize normally.
+})(globalThis);
 // vim: set expandtab shiftwidth=4 tabstop=4:
